# Base De Datos

La aplicacion utiliza PostgreSQL.

## Archivos

- `schema.sql`: estructura de tablas, relaciones y restricciones.
- `seed.sql`: datos iniciales de referencia.

La carga completa para la demostracion se ejecuta desde los scripts ubicados en `backend/database/`.
